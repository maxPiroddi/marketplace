<!--
<%= simple_form_for @product do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.input :name %>
  <%= form.input :price, as: "integer", class: "input-num" %>
  <%= form.input :location, label: "Postcode", as: "integer", class: "input-num" %>
  <%= form.input :description %>
  <%= form.input :pictures, as: "file", accept: "image/png,image/gif,image/jpeg,image/jpg" %>
  <%= form.button :submit, class: "btn btn-default" %>
<% end %>
-->
<%= form_with model: product, local: true do |form| %>

    <% if product.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

        <ul>
          <% product.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
        <%= form.label :name %>
        <%= form.text_field :name %>
    </div>
    <div>
        <%= form.label :location %>
        <%= form.number_field :description, min: 1000, max: 4999 %>
    </div>
    <div>
        <%= form.label :price, "Price (in cents)" %>
        <%= form.number_field :price, min: 0 %>
    </div>
    <div>
        <%= form.text_area :description %>
    </div>
    <div>
        <%= form.label :picture %>
        <%= form.file_field :picture, accept: "image/png,image/gif,image/jpeg,image/jpg" %>
    </div>
    <%= form.submit %>
<% end %>
